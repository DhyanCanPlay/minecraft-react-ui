import cn from "classnames";
import React from "react";
import "./Logo.css";

type Props = {};

const Logo = (props: Props) => {
  return <div className={cn("Logo")}>UI</div>;
};

export default Logo;
